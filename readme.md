Simple Rag application using Gemini, Pinecone, SentencTransformer for Embedding

```bash
python api.py  
```

```bash
 streamlit run streamlit_app.py
 ```

```bash
uvicorn api:app --host 0.0.0.0 --port 8000
```
